import React from 'react';
import Layout from "../Layout";
import OnionVersus from "../onionversus/OnionVersus";

function Home(props) {
    return (
        <Layout>
            <OnionVersus/>
        </Layout>
    );
}

export default Home;